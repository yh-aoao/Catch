# Roll 恢复与早期 Basket 历史核对

## Roll

目标为 645edc4e21631683254fba51cc66b035b409518d（2026-09-11 11:40:30 +0800）。
对当前 ROLL_645EDC4_MANIFEST.json 中的 11 个文件逐一读取目标 Git blob，应用记录的
私有 import 替换后比较 AST，全部一致。默认 Roll 构造及 train_DCMM.py PPO 选择
已路由至该版本；因此本次没有再修改其奖励、物理或终止规则。
roll_log=true 新增打印实际旧环境与配置文件路径，帮助核对服务器运行版本。
普通 DcmmVecEnv.py 中保留的新 Roll 方法不是默认 Roll 实例实际执行的方法。
仍需用户提供回退后的具体训练命令及故障信息，不能把旧策略效果差当作代码不一致的证据。

## Basket 候选（本地 Git 记录，时间不是已核实的 GitHub 推送时间）

| 提交 | 时间 | 相关情况 |
|---|---|---|
| 7b223d9 | 2026-07-31 11:43 | 初次加入抛球入篮模式 |
| 19a9176 | 2026-08-02 14:44 | 固定底座、握球与手指自由控制 |
| 857d66a | 2026-08-07 11:46 | 固定底座，加入手指关节链奖励，最接近用户描述的臂手联合训练 |
| 912cdea | 2026-08-07 16:03 | 改为杯状手姿、手动作缩放 0.1，删除手指奖励，偏向训练臂 |

已核对以上四个版本的实际代码：持球结束后均有
throw_boost = np.array([0.0, 1.5, 2.0])，并将其加到取出的手掌线速度上，
随后通过 set_throw_pos_vel 写入球速度。默认持球时长为 0.3 秒。
部分版本还在臂/底座动作接近零时启动 demo 臂动作。
因此必须区分“固定底座训练架构可参考”和“旧球飞行是纯物理学习结果”这两件事。
857d66a 的提交标题提到去掉硬编码释放推手，但其实际球速度加成仍然存在。

## 与当前 Basket 的区别及后续建议

当前 Catching 必须先达到 parking 的位置和速度条件，进入 preparing 后才开放抛球控制；
该阶段球受重力和接触驱动，没有旧速度加成。因此新旧任务难度、数据分布不同。
目前尚无此次 Basket 的 phase、ik_successes、hand_contact 等运行日志，不能确定是
停车未完成、IK 失败、初始球未稳定接触手，还是奖励不足。

建议先做独立的固定底座单阶段物理抛球实验：初态确认球受手支撑，直接控制臂手，
首先对固定篮筐训练，再逐步扩展篮筐位置。保留当前物理出手与过框判定；
旧版可参考臂手控制和奖励项，不能恢复给球加速度来声称策略已经学会抛球。
确认该单阶段专家有效后，再接回停车阶段。
本次仅完成历史审计及 Roll 运行路径诊断，没有擅自回退 Basket 为脚本抛球。
